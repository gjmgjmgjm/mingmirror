# Development / test dependencies for douyin-downloader.
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest>=7.0
pytest-asyncio>=0.21
ruff>=0.4.0
httpx>=0.25
hypothesis>=6.0
