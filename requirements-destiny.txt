# Optional dependencies for destiny analysis subsystems (ziwei, qizheng, bazi ensemble).
#
# Core dependencies (aiohttp, aiofiles) are already listed in requirements.txt.
# Install core dependencies first:
#   pip install -r requirements.txt
#
# Optional enhancement:
#   sentence-transformers  # enables dense semantic retrieval in tools/destiny/strategies/retriever.py
